import language from "assets/language/language.json"

const initValue = {
	languageText: language
}

const languageTextReducer = (state = initValue, action) => {
	return state
}

export default languageTextReducer
